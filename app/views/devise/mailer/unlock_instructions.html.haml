%p
  = t(".hello", :email => @resource.email )
%p= t(".lock_msg")
%p= t(".click_link_msg")
%p= link_to t(".unlock_my_acc"), unlock_url(@resource, :unlock_token => @resource.unlock_token)
%p Regards,
U-IN Team
